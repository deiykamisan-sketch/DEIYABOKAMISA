const canvas=document.querySelector('#board');
if(canvas){const ctx=canvas.getContext('2d');let drawing=false,last=null,erase=false;
function point(e){const r=canvas.getBoundingClientRect(),t=e.touches?.[0]||e;return{x:(t.clientX-r.left)*canvas.width/r.width,y:(t.clientY-r.top)*canvas.height/r.height}}
function start(e){drawing=true;last=point(e);e.preventDefault()} function move(e){if(!drawing)return;const p=point(e);ctx.beginPath();ctx.moveTo(last.x,last.y);ctx.lineTo(p.x,p.y);ctx.lineCap='round';ctx.lineJoin='round';ctx.lineWidth=document.querySelector('#width').value;ctx.strokeStyle=erase?'#fff':document.querySelector('#color').value;ctx.stroke();last=p;e.preventDefault()} function stop(){drawing=false}
['mousedown','touchstart'].forEach(x=>canvas.addEventListener(x,start));['mousemove','touchmove'].forEach(x=>canvas.addEventListener(x,move));['mouseup','mouseleave','touchend'].forEach(x=>canvas.addEventListener(x,stop));
document.querySelector('#eraser').onclick=()=>{erase=!erase;document.querySelector('#eraser').textContent=erase?'Pen':'Eraser'};document.querySelector('#clear').onclick=()=>ctx.clearRect(0,0,canvas.width,canvas.height);
document.querySelector('#save').onclick=async()=>{const s=document.querySelector('#status');s.textContent='Saving…';const res=await fetch(window.snapshotUrl,{method:'POST',headers:{'X-CSRFToken':window.csrfToken,'Content-Type':'application/json'},body:JSON.stringify({image:canvas.toDataURL('image/png'),recognized_text:document.querySelector('#recognized').value,drawing_type:document.querySelector('#drawing-type').value})});s.textContent=res.ok?'Saved ✓':'Could not save';if(res.ok)setTimeout(()=>location.reload(),500)};}
